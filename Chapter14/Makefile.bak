APUE := $(shell dirname $(shell pwd))

INCS=$(APUE)/apue.3e/include
LIBS=-L$(APUE)/apue.3e/lib -lapue
CFLAGS=-I$(INCS) $(LIBS)

objs=write deadlock mcopy2 mandatory

all : $(objs)
	mv $(objs) $(APUE)/bin

# %.o : %.c

clean:
	cd $(APUE)/bin && rm $(objs)
